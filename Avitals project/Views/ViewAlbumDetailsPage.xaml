<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Avitals_project.Views.ViewAlbumDetailsPage"
             xmlns:viewmodels="clr-namespace:Avitals_project.ViewModels"
             x:DataType="viewmodels:ViewAlbumDetailsPageViewModel"
             Title="ViewAlbumDetailsPage"
             BackgroundColor="{StaticResource MainColor}">
    <VerticalStackLayout>
        <Grid RowDefinitions="20,*" ColumnDefinitions="Auto,*,Auto" Padding="40,70,40,30">
            <HorizontalStackLayout Grid.Row="0">
                <Label Text="{Binding Album.AlbumTitle}" TextColor="White" FontSize="Title"></Label>
            </HorizontalStackLayout>
        </Grid>
        <Frame Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" HeightRequest="700" BackgroundColor="White" CornerRadius="45">
            <VerticalStackLayout>
                <Image Source="{Binding Album.AlbumCover }" WidthRequest="200" HeightRequest="120" Aspect="AspectFill"></Image>
            </VerticalStackLayout>
            </Frame>
    </VerticalStackLayout>
</ContentPage>